kind: Pod
apiVersion: v1
metadata:
  name: mypod
spec:
  containers:
    - name: testfrontend
      image: nginx
      volumeMounts:
      - mountPath: "/usr/share/nginx/html/"
        name: mypd
  volumes:
    - name: mypod
      persistentVolumeClaim:
        claimName: myclaim

